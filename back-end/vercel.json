{
  "version": 2,
  "env": {
    "DATABASE_URL": "postgresql://Patrick:1234@localhost:5432/contact_mgmt_back?schema=public"
  },
  "builds": [
    {
      "src": "src/main.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/main.ts",
      "methods": ["GET", "POST", "PATCH", "DELETE"]
    }
  ]
}
